# 2026-08-13 重大失误专项复盘

## 事故结论

本次部署工作属于重大失误。

在部署链路已经连续失败、且没有获得新的有效证据后，我仍然反复尝试 Git 推送、插件上传和网页上传。最终既没有完成部署，也没有准确定位“添加到主屏幕后账号输入框无法点击”的原因，浪费了用户约半小时的时间和额外 Token，并显著增加了用户的操作负担与挫败感。

本次问题的责任在执行和判断流程，而不是用户操作。

## 实际发生了什么

1. 本地构建和提交已经完成，但远程部署链路并未打通。
2. Git 推送出现代理或连接重置问题。
3. GitHub 写入通道出现权限失败。
4. 网页上传又发生超时。
5. 在这些失败已经表明“当前上传链路不可用”后，我没有及时停止，而是继续更换方式重复尝试。
6. 对主屏幕输入框问题，我没有先完成可复现验证，就过早把问题归因到页面代码或 HTTPS 环境。
7. 用户随后已经可以正常输入账号，这说明我此前并没有真正确认问题根因，也不能把恢复归功于任何未部署的修改。

## 对用户造成的影响

- 浪费约半小时等待时间。
- 消耗了不必要的 Token 和沟通成本。
- 让用户重复进行本不该由用户承担的后台、终端和授权操作。
- 没有交付部署结果，却持续汇报尝试过程。
- 对问题作出了未经验证的判断，干扰了正确排查方向。
- 降低了用户对后续开发、部署和故障处理的信任。

## 根本原因

### 1. 没有设置部署止损线

同类网络错误发生后，我把“再试一次”当成推进，而没有判断新的尝试是否真的提供了不同的成功路径。

### 2. 混淆了构建成功与部署成功

本地构建、生成提交、推送远端、触发 GitHub Pages、线上资源更新是五个不同阶段。本次只完成了前两项，却在沟通中让部署流程显得仍然可继续完成。

### 3. 多个方案实际上共用同一故障链路

Git、CLI 和网页上传看似是不同方案，但都受当前代理、连接或权限环境影响。在没有先做小规模连通性验证的情况下轮流尝试，本质上只是重复失败。

### 4. 没有先复现主屏幕输入问题

针对 iOS 主屏幕 PWA 的输入问题，应先确认具体安装实例、缓存版本、遮挡层、焦点事件和输入框可点击区域，再判断是否需要改代码。本次缺少这一证据链。

### 5. 过程汇报多，结果控制不足

我花了过多时间描述尝试和引导用户操作，却没有及时给出最重要的结论：当前部署链路不可用，应立即停止。

## 正确的处理方式

当首次推送失败后，最多只应进行一次有明确差异的复核：

1. 先确认本地构建是否成功，并保留本地提交。
2. 用一个最小请求验证远程连接和权限。
3. 如果第二次仍为同类网络或权限错误，立即停止部署。
4. 明确告诉用户：代码已在本地准备好，但本次没有部署成功。
5. 不再要求用户反复打开后台、创建令牌或切换上传方式。
6. 主屏幕输入问题必须独立复现，不能与部署问题混在一起推断。

## 今后强制执行的止损规则

### 部署规则

- 同一部署任务最多尝试两次，或最多占用五分钟；任一条件达到就停止。
- 第二次尝试必须与第一次具有明确不同的成功依据，禁止原样重试。
- 在代理或连接异常时，先做最小连通性检查，不直接上传完整构建包。
- 必须分别记录：构建、提交、推送、Pages 构建、线上验证五个状态。
- 只有远程提交存在且线上资源版本验证通过，才能说“部署完成”。
- 用户说“停止”或“不用部署”，立即停止所有上传、推送和后台操作。

### 排查规则

- 没有稳定复现，不下根因结论。
- 问题自行恢复时，结论应写为“原因尚未确认”，不能把恢复归因于未部署代码。
- 先检查交互遮挡、缓存和运行环境，再修改业务代码。
- 每次修改前写清楚：证据、假设、验证方法和成功标准。

### Token 与时间控制规则

- 首次重复失败后，不再展开多条未经验证的旁路方案。
- 优先报告明确事实和下一步，不用大量文字包装失败。
- 不让用户承担与当前目标无关的配置和授权操作。
- 如果无法在当前环境完成，及时交付可恢复状态和准确阻塞原因。

## 当前真实状态

- 本次没有完成线上部署。
- 所有部署操作已经停止。
- 本地准备的修改仍保留在本地，不应声称已上线。
- 用户现在已经可以输入账号，但恢复原因尚未确认。
- 在用户再次明确授权前，不进行任何部署、推送或线上修改。

## 自我改进结论

这次最大的错误不是一次命令失败，而是在失败已经足够明确之后仍然继续消耗用户的时间。以后处理部署问题时，首要目标不是“不断尝试”，而是快速确认当前链路是否具备成功条件；不具备时立即止损，并保持事实准确。

后续每次部署都必须以“可验证、可停止、可回退”为前提。任何未经验证的判断，都不能再当作确定结论告诉用户。

## 2026-08-14 补充事故：银河文件发布到错误目录

### 事故结论

本次属于重大部署失误。手机实际读取仓库根目录的 `memory-galaxy/`，我却连续把新版文件上传到 `public/memory-galaxy/`。GitHub Pages 每次都显示构建成功，但用户使用的真实页面没有发生变化。我随后又多次把问题归因于手机缓存，并要求用户反复退出和刷新，判断方向错误，增加了用户的挫败感。

### 根本原因

- 混淆了本地 Vite 工程的 `public/` 目录与 GitHub Pages 从仓库根目录发布后的线上路径。
- 只把“提交成功、Pages 成功”当成部署完成，没有立即读取 `4ta.space/memory-galaxy/index.html` 验证真实响应。
- 已知 20:23 的成功提交更新的是根目录 `memory-galaxy/`，却没有先复用该提交方式进行对照。
- 在证据不足时把问题推给缓存，而没有先比较仓库路径与手机请求路径。

### 对用户造成的影响

- 多次显示部署成功，但用户始终看不到更新。
- 让用户重复刷新、退出和重新进入，承担了本不该由用户承担的排错成本。
- 浪费时间与 Token，降低了对部署结果的信任。

### 新增强制规则

1. 银河正式部署只允许写入仓库根目录 `memory-galaxy/`；`public/memory-galaxy/` 仅是本地构建输入，不是线上目标。
2. 发布前先对照最近一次已知成功提交的文件路径，不自行推断部署目录。
3. Pages 工作流成功后，必须直接请求真实域名对应 URL，并核对 HTML 中的新 JS、CSS 哈希和关键文案。
4. 未完成真实域名响应核验前，不得向用户报告“部署完成”。
5. 手机内容未更新时，先核对线上响应和资源路径；只有线上已确认是新版而设备仍为旧版时，才进入 PWA/CDN 缓存排查。
